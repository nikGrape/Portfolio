import { motion } from 'framer-motion';
import { styled } from 'styled-components';

export const Contact = styled(motion.div)`
	margin: 12px;
	user-select: none;

	/* a {
		box-shadow: 0px 5px 30px 1px #cc6e6e;
		color: white;
		font-weight: 600;
	} */
`;
